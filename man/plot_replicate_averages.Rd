% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_replicate_averages}
\alias{plot_replicate_averages}
\title{Plot Average Posterior Means Across Replicates with True Trend}
\usage{
plot_replicate_averages(results_multi, sims_list, title = NULL)
}
\arguments{
\item{results_multi}{List from \code{consolidate_results()} with \code{by_replicate=TRUE}.}

\item{sims_list}{List of simulation objects (one per replicate), each containing
Y, z, mu, and years.}

\item{title}{Optional plot title (default NULL).}
}
\value{
ggplot2 object.
}
\description{
Creates a plot showing the average posterior mean for each fitted H value
across multiple replications, overlaid with the true trend (mu) and simulated data.
}
\details{
The function:
\enumerate{
\item Computes the average posterior mean for each H across all replicates
\item Plots these averages as colored lines
\item Overlays the true trend (mu) as a red line
\item Shows the averaged Y (climate models) from each replicate as grey lines
}
}
\examples{
\dontrun{
results <- consolidate_results(fits_list, sims_list, by_replicate = TRUE)
p <- plot_replicate_averages(results, sims_list)
print(p)
}

}

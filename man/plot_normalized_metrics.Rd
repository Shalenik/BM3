% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_normalized_metrics}
\alias{plot_normalized_metrics}
\title{Plot Normalized Metrics with Elbow Detection}
\usage{
plot_normalized_metrics(model_comparison, title = NULL, show_legend = TRUE)
}
\arguments{
\item{model_comparison}{Tibble with columns: H, RMSE, mean_post_var, max_post_var
(typically from \code{consolidate_results()$replicate_comparison}).}

\item{title}{Optional plot title (default NULL).}

\item{show_legend}{Logical. Whether to display legend (default: TRUE).}
}
\value{
ggplot2 object showing normalized metrics overlaid with elbows marked by vertical lines.
}
\description{
Creates an overlay plot of RMSE, mean posterior variance, and max posterior variance,
all normalized to \link{0,1} scale with error bars. Displays elbow points for each metric.
}
\details{
The function:
\enumerate{
\item Normalizes all three metrics to \link{0,1} scale
\item Computes standard errors if available (sd columns)
\item Identifies elbow points for each metric independently
\item Displays metrics as lines with error bars
\item Marks elbows with colored vertical dotted lines:
\itemize{
\item Blue: RMSE elbow
\item Green: Mean posterior variance elbow
\item Orange: Max posterior variance elbow
}
}
}
\examples{
\dontrun{
results <- consolidate_results(fits, sims, by_replicate = TRUE)
p <- plot_normalized_metrics(results$replicate_comparison)
print(p)
}

}

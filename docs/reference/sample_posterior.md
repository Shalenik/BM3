# Sampled Posterior Curves

Returns a sample of posterior curves from a BFACT object.

## Usage

``` r
sample_posterior(BFACT, ncurves = NULL)
```

## Arguments

- BFACT:

  A BFACT object with a \$ysim element.

## Value

Numeric matrix of posterior curves.

# Compute Posterior Mean Curve

Returns the posterior mean curve from a BFACT object.

## Usage

``` r
posterior_mean_curve(BFACT)
```

## Arguments

- BFACT:

  A BFACT object with a \$ysim element.

## Value

Numeric vector of posterior means.
